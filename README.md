# 장중 시세 원인 분석

국내 주식 종목을 입력하면 **오늘 왜 그렇게 움직이는지** 즉답하는 앱.

두 가지 형태로 쓸 수 있다.

| | 형태 | 셋업 | 데이터 |
|---|---|---|---|
| **웹앱** | 브라우저 입력창 | Python + API 키 | 네이버 금융 |
| **스킬** | Claude 대화창 | 없음 | 세션에 연결된 MCP |

---

## 핵심 아이디어

"왜 움직였나"에 답하기 전에 **"누구 탓인가"를 숫자로 먼저 확정한다.**

시장이 다 빠져서 같이 빠진 종목에 개별 재료를 갖다 붙이면 그건 그냥 그럴듯한
헛소리다. 그래서 뉴스를 읽기 전에 등락률을 세 성분으로 쪼갠다.

```
종목등락 = 시장성분 + 업종초과성분 + 종목고유성분

시장성분     = beta x 지수등락
업종초과성분 = 업종등락 - beta x 지수등락
종목고유성분 = 종목등락 - 업종등락
```

세 성분의 합은 **항상** 종목 등락률과 정확히 일치한다(항등식이라 검산 가능).
절대값이 가장 큰 성분이 주도 요인이고, 2순위도 유의미하면 함께 보고한다.

여기에 **타이밍 판별**이 붙는다. 갭(전일종가→시가)이 큰지, 장중 이동이 큰지를
갈라서 재료가 개장 전에 나왔는지 장중에 터졌는지 특정한다. 장중 급락인데 오전
9시 기사를 원인으로 대면 틀린 답이기 때문이다.

이 정량 분석이 끝난 뒤에야 LLM이 뉴스를 읽고 **원인에 이름을 붙인다.** LLM은
분해 결과를 뒤집지 못하고, 근거를 못 찾으면 "확인된 재료 없음"이라고 답하도록
프롬프트에 못박혀 있다.

### 실제 출력 예시

2026-08-06 장중, 코스피 급락일:

```
삼성전자 (005930)  228,500원  ▼ -17,500 (-7.11%)

■ 삼성전자 -7.11% 급락 — 주 원인은 KOSPI -4.97%에 연동된 시장 전체 흐름(-4.97%p);
  여기에 전기·전자 업종이 시장보다 더 깊게 하락(-3.68%p)이 겹쳤습니다.

[등락률 분해]
  시장     -4.97%p (49%)
  업종초과 -3.68%p (36%)
  종목고유 +1.54%p (15%)     <- 업종 대비로는 오히려 선방

[정황]
  · 시가 대비 -5.28% 이동 — 장중에 재료가 발생했습니다.
  · 당일 변동폭 7.3%p로 매우 넓습니다(고가 246,000 / 저가 228,000).
  · 저가 부근에서 거래 중 — 매도 압력이 아직 해소되지 않았습니다.
  · 동종 종목: SK하이닉스 -10.19%
```

---

## 웹앱 실행

```bash
cd app
pip install -r requirements.txt
cp .env.example .env      # ANTHROPIC_API_KEY 입력
```

먼저 데이터 소스가 살아있는지 확인한다.

```bash
python -m server.cli --selftest
```

네이버는 공식 시세 API가 아니라서 스키마가 바뀔 수 있다. 이 명령이 어느
엔드포인트가 죽었는지 정확히 알려준다.

```bash
# 웹 서버
uvicorn server.main:app --port 8000
# -> http://localhost:8000

# 터미널에서 바로
python -m server.cli 삼성전자
python -m server.cli 005930 --no-llm      # LLM 없이 규칙 기반만
python -m server.cli 한화오션 --json
```

### API

```
GET /api/why?q=삼성전자        JSON
GET /api/why.txt?q=005930     텍스트 (curl 용)
GET /api/health
```

`&llm=false` 를 붙이면 LLM 없이 규칙 기반 결과만 돌려준다.

### 환경 변수

| 변수 | 기본값 | 설명 |
|---|---|---|
| `ANTHROPIC_API_KEY` | (없음) | 없으면 규칙 기반으로만 동작한다. 앱은 그대로 돌아간다. |
| `STOCKWHY_MODEL` | `claude-sonnet-5` | 장중 즉답이라 지연시간 우선. 더 깊은 판단이 필요하면 `claude-opus-5`. |

---

## 스킬 사용 (Claude 대화창)

`.claude/skills/stock-why/` 에 들어 있다. 셋업 없이 바로 쓸 수 있다.

```
/stock-why 삼성전자
```

또는 그냥 "삼성전자 왜 빠져?" 라고 물어도 된다. 웹앱과 동일한 분해 방법론을
쓰되 데이터는 세션에 연결된 MCP(`koreaStock`, `NaverSearch`)에서 가져온다.

---

## 구조

```
app/
  server/
    models.py                  도메인 모델
    peers.py                   업종/테마별 대표 종목 맵
    pipeline.py                수집 -> 분해 -> 서술
    cli.py                     CLI + --selftest 진단
    main.py                    FastAPI
    providers/naver.py         네이버 수집 (다중 엔드포인트 폴백)
    analysis/
      attribution.py           등락률 분해, 타이밍 판별, 뉴스 스코어링
      llm.py                   Claude 서술
    static/index.html          프론트엔드
  tests/                       53개
.claude/skills/stock-why/      대화창용 스킬
```

```bash
cd app && python -m pytest    # 53 passed
```

테스트는 2026-08-06 실제 장중 시세를 픽스처로 쓰고, 네이버 응답은
`httpx.MockTransport` 로 흉내내서 네트워크 없이 전 경로를 검증한다.

---

## 알아둘 것

**네이버 엔드포인트는 비공식이다.** 모바일 웹이 쓰는 경로라 안정적으로
동작하지만 공식 계약이 아니다. 그래서 provider 는 엔드포인트마다 후보를 여러 개
두고, 일부가 죽어도 예외 대신 결측으로 흘려보내며(분석 단계가 결측을 감당한다),
무엇이 실패했는지 `diagnostics` 에 남긴다. 이상하면 `--selftest` 부터 돌린다.

**업종 등락률은 근사값이다.** 네이버 업종 지수 엔드포인트가 신뢰도가 낮아서
`peers.py` 의 테마별 대표 종목을 시가총액 가중 평균해서 쓴다. 피어를 못 찾으면
업종 성분은 0이 되고 잔차는 전부 종목고유로 흡수된다 — 이 경우 확신도가
자동으로 낮아진다.

**연합인포맥스는 붙이지 않았다.** 유료 구독 기반이라 공개 스크래핑으로는
장중 속보를 안정적으로 못 가져온다. 대신 네이버 금융의 공시성 뉴스
(`market_get_news` 계열)가 장중 속보를 상당 부분 커버한다. 인포맥스 구독이
있다면 `providers/` 에 같은 인터페이스로 추가하면 된다.

**투자 판단에 쓰지 말 것.** 원인 설명까지가 이 앱의 범위다. 매수/매도 의견은
내지 않도록 프롬프트에서 명시적으로 금지했다.
