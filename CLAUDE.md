# 리포지토리 안내

한국 국채선물 차익거래용 엑셀 워크북과 그 생성·검증 스크립트를 담고 있다.
작업 디렉터리는 `ktb_arb/`. 상세 문서는 `ktb_arb/README.md`.

## 환경

```bash
cd ktb_arb
pip install -r requirements.txt      # openpyxl, python-dateutil, formulas
```

Python 3.11 기준.

## 핵심 규칙

**엑셀 파일을 직접 수정하지 말 것.** `.xlsx` 는 전부 생성물이다.
모든 변경은 생성기 스크립트에서 하고 재생성한다.

두 가지 버전이 있다. **실사용은 간소판**(`국채선물_차익거래_간소.xlsx`,
시트 2개·2종목·인포맥스 IMDP 실수식)을 쓴다.

```bash
python build_simple.py      # 간소판 재생성  (권장)
python compare_simple.py    # 간소판 검증

python build_ktb_arb.py     # 원본판(9시트·3종목) 재생성
python audit_wiring.py      # 원본판 배선 감사 (수초)
python compare_sol.py       # 원본판 검증 (수분)
```

워크북을 수정했으면 해당 검증이 `=== 판정: PASS ===`를 찍어야 커밋한다.

간소판은 사용자의 기존 `KTB10 ... v2.xlsm` 을 참조해 만들었다.
그 파일의 분석 결과는 `ktb_arb/기존파일_분석.md` 에 있다 — 캐리 이중계상,
이표일 하드코딩 스테일, `#VALUE!` 선도가격, 선물 듀레이션 인자 오류를
발견해 간소판에서 전부 고쳤다.

## 검증 경로에 대한 주의

`openpyxl`은 수식을 문자열로만 쓰고 캐시값을 남기지 않는다. 따라서
갓 생성한 파일을 `data_only=True`로 읽으면 전부 `None`이다.

- `compare_sol.py` — `formulas` 라이브러리가 수식을 직접 계산한다. **LibreOffice 불필요.**
  개발 컨테이너의 LibreOffice가 고장나 있어(6셀 파일도 "source file could not be loaded")
  이 경로가 주 검증 수단이다.
- `verify.py` — 캐시값이 채워진 파일에서만 동작한다. 엑셀로 열어 저장했거나
  LibreOffice recalc를 거친 뒤에만 쓸 것.

`formulas`는 `=`로 시작하는 **문자열**을 수식으로 파싱해 에러를 낸다.
설명·예시 텍스트가 `=`로 시작하면 `build_ktb_arb.py`의 `as_text()`로
`data_type='s'`를 강제해야 한다. Excel에서도 같은 이유로 `#NAME?`가 난다.

## 워크북 레이아웃 상수

`build_ktb_arb.py` 상단:

- `NPER = 62` — 반기 이표 행 수 (30년물 커버)
- `SCH0, SCH1 = 15, 76` — `현금흐름` 시트 스케줄 행 범위
- `BLOCK_STARTS = [1, 43, 85]` — 종목별 블록 시작 열 (블록폭 40)
- `CFV = {"C": "B", "D": "AR", "E": "CH"}` — `바스켓` 열 → `현금흐름` 스칼라 값 열

바스켓 종목을 3개에서 늘리려면 이 넷과 `바스켓` 시트의 `"CDE"` 루프를 함께 고쳐야 한다.

## 도메인 규약

- 채권 단가는 **한국 관행식**(단수기간 단리 + 이후 반기복리)으로 계산하며 결과는 **더티**다.
  엑셀 `PRICE()`는 단수기간도 복리 할인하므로 쓰지 않는다.
- 손익계산은 전 구간 더티 기준으로 일관한다. 경과이자·클린가는 참고 표시용일 뿐
  계산 경로에 넣지 않는다.
- 시나리오/만기 현물평가는 테일러 근사가 아니라 **shift별 정밀 재할인**을 쓴다.
  근사로 되돌리지 말 것 — ±100bp에서 오차가 락인 차익과 맞먹었다.
- 선도수익률 역산은 **뉴턴 2회**를 유지할 것. 1회로 줄이면 shift 0 에서
  수렴 잔차가 현물 leg 로 누수되는데(계약당 311원), 락인 차익이 계약당 626원
  수준이라 신호의 절반을 먹는다. 컬럼 2개면 해결된다.
- 캐리에 쓰는 유효금리는 `메인!B31` 하나로 모은다:
  `IF(방향계수=1, 조달금리 B12, 담보운용금리 B13) − 대차수수료 B23/100`.
  엔진은 이 셀만 받아간다. 대차수수료는 현물을 빌려 파는 쪽에서 발생하며,
  반대 방향에서는 special 프리미엄으로 해석돼 부호가 자동으로 맞는다.
- 이론가는 반드시 **선도**금리로 계산한다. 현물금리로 내면 그 이론가와 시장가의
  차이가 곧 캐리라서, 캐리를 따로 더하면 이중계상이 된다(원본 xlsm 의 실제 결함).

## 워크북 레이아웃 상수 — 간소판

`build_simple.py` 상단: `SHIFTS`(시나리오 9개), `NPER = 62`, `SCH0 = 15`,
`BLOCKS = [1, 31]`(블록폭 `NCOL = 28`), `EV = {"C": "B", "D": "AF"}`.
종목 수를 바꾸려면 이 넷과 `메인` 시트의 `"CD"` 루프를 함께 고친다.

## 현재 상태

- 간소판: `compare_simple.py` PASS. 수식 3,588개, 오류 0,
  시나리오 9개 shift 전체 오차 0.0000원, shift0 누수 계약당 0.8원.
  종목코드·수익률은 사용자 기존 xlsm 에서 가져온 실제 값이다.
- 원본판: `compare_sol.py` PASS. 수식 7,847개, 시나리오 최대오차 0.0848원.
  `실시간` 시트 값과 `바스켓` 4~8행은 **더미**다.

주의: 현재 시장 수준에서 락인 차익(계약당 약 627원)보다 ±100bp 컨벡시티
손실(계약당 2.4~2.8만원)이 훨씬 크다. 이 비대칭을 사용자에게 숨기지 말 것.

남은 작업은 `ktb_arb/README.md` 8절 참조.
