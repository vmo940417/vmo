# 국채선물 차익거래 실시간 모니터 (10년물)

바스켓 채권의 실시간 수익률과 국채선물 실시간 가격으로부터 **바스켓 1단위(= 선물 1계약)당
만기 손익**을 실시간 산출하는 엑셀 워크북과, 그 워크북을 생성·검증하는 스크립트.

산출물: `국채선물_차익거래.xlsx`

---

## 1. 빠른 시작 (데스크탑)

```bash
git clone https://github.com/vmo940417/vmo.git
cd vmo
git checkout claude/korean-bond-futures-arbitrage-i2ttpb
cd ktb_arb

python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

엑셀만 쓸 거면 `국채선물_차익거래.xlsx`를 바로 열면 된다. 수식을 고칠 때는
**엑셀에서 직접 고치지 말고** `build_ktb_arb.py`를 수정한 뒤 재생성한다.

```bash
python build_ktb_arb.py          # → 국채선물_차익거래.xlsx 재생성
python audit_wiring.py           # 참조 배선 감사 (수초)
python compare_sol.py            # 수식 값 평가 + 독립 모델 대조 (수분)
```

---

## 2. 파일 구성

| 파일 | 역할 |
|---|---|
| `build_ktb_arb.py` | **워크북 생성기. 모든 수정은 여기서 한다.** |
| `국채선물_차익거래.xlsx` | 생성물 (git에 커밋되어 있음) |
| `audit_wiring.py` | 참조 배선 감사 — 없는 시트/빈 셀 참조, 순환참조, 핵심 참조의 대상 행 라벨 일치 |
| `compare_sol.py` | `formulas` 라이브러리로 워크북을 실제 평가해 파이썬 독립 모델과 대조 |
| `verify.py` | Excel/LibreOffice가 캐시값을 채운 뒤 그 값을 읽어 대조하는 경로 |
| `requirements.txt` | 의존성 (Python 3.11 기준) |

### `compare_sol.py` vs `verify.py`

- `compare_sol.py` — **LibreOffice 불필요.** `formulas` 라이브러리가 수식을 직접 계산한다.
  개발 컨테이너에서 LibreOffice가 고장나 있어 이 경로를 주 검증 수단으로 썼다.
- `verify.py` — 워크북에 **캐시값이 들어 있어야** 동작한다 (`data_only=True`로 읽음).
  openpyxl이 갓 생성한 파일은 캐시값이 없어 전부 `None`이다.
  데스크탑에서 엑셀로 파일을 열고 저장하거나 LibreOffice로 recalc한 뒤 실행할 것.

---

## 3. 워크북 시트 구조

| 시트 | 내용 |
|---|---|
| `README` | 사용설명서, 계산 규약, 반영하지 않는 것 |
| `실시간` | **RTD/DDE 연결 셀 모음.** 붉은 셀에 벤더 수식을 붙여넣는다 |
| `설정` | 계약사양 / 결제일정 / 금리 / 포지션 / 거래비용. **노란 셀만 입력** |
| `바스켓` | 최종결제기준채권 3종목의 현물·선도 평가, BPV, 헤지액면 |
| `이론가` | 표준물 가격, 이론 선물가, 선물 내재수익률, 베이시스, 차익 판정 |
| `손익` | [A] 실시간 락인 손익 [B] 현재 미실현 [C] 만기 손익 |
| `시나리오` | 만기 수익률 −100 ~ +100bp 평행이동 손익표 (헤지 유효성 점검) |
| `현금흐름` | **계산 엔진.** 종목별 이표 스케줄 + 단가 계산 + 선도수익률 뉴턴해. 직접 수정 금지 |
| `연동가이드` | 벤더별 RTD/DDE 수식 예시, 준비물 체크리스트, 잔여 리스크 |

셀 색상: 노란 배경 = 사용자 입력 · 하늘색 배경 = 실시간 연결 · 초록 글씨 = 타시트 참조 · 연두색 = 핵심 결과

---

## 4. 계산 모형

한국 국채선물은 **현금결제**이고, 최종결제기준가격은 바스켓 종목 수익률의 단순평균을
표준물(표면금리 5%, 6개월 이표, 잔존 n년 가상채권)의 할인율로 넣어 산출한다.

```
만기손익 = [선물 leg]  (진입가 − 표준물가격(바스켓 평균수익률)) × 1,000,000 × 계약수 × 방향
         + [현물 leg]  Σ (만기 처분단가 − 선도 목표단가) / 100 × 보유액면 × 방향
         − 거래비용

선도 목표단가 = 현물단가 + 조달이자 − 기간중 쿠폰 재투자원리금
```

- **채권 단가**: 한국 관행식 `P = [Σ CF_k /(1+y/2)^(k-1)] / (1 + y/2 × d/T)`.
  단수기간 단리, 이후 반기복리. 결과는 경과이자를 포함한 **더티(단가)** 이며
  모든 손익계산을 더티 기준으로 일관한다.
  엑셀 `PRICE()`는 단수기간도 복리 할인해서 수십 전 차이가 나므로 쓰지 않는다.
- **표준물**: 잔존 정확히 n년 → 단수기간 없음 → 폐쇄형
  `P = (5/y)(1−v^N) + 100·v^N`, `v = 1/(1+y/2)`, `N = 2n`
- **선도수익률**: 현물 더티단가를 조달금리로 캐리시킨 선도 목표단가에 대해 뉴턴법 2회로 역산.
  수렴 잔차는 `바스켓` 38행에서 확인 (~1e-9).
- **헤지액면**: `w_i × (표준물BPV / 선도BPV_i) × 거래단위`.
  종목별 수익률 1bp 변동이 선물 leg와 정확히 상쇄되도록 잡는다.
- **시나리오/만기 현물평가**: 테일러 근사가 아니라 **shift별 정밀 재할인**.
  `현금흐름` 시트에 종목당 22개(손익 1 + 시나리오 21) 재할인 열이 있다.

---

## 5. 검증 상태

`compare_sol.py` 최종 실행 결과 — **PASS**

| 항목 | 차이 |
|---|---|
| 오류값(#REF!/#VALUE!/#NAME? 등) 셀 | 0개 |
| 수식 셀 / 순환참조 | 7,847개 / 0건 |
| 현물단가 · 선도 목표단가 (3종목) | 0.00e+00 |
| 선도수익률 | < 1e-9 |
| 선도 BPV | < 7e-12 |
| 이론 선물가격 | 7.7e-09 |
| 시나리오 21개 shift 전체 | 최대 0.0848원 |

잔여 오차 0.08원은 선도수익률 뉴턴 잔차(~1e-9)가 액면 1.2억원에 전파된 수준.

---

## 6. 알아둘 것

### 컨벡시티 잔여 리스크는 버그가 아니다

`시나리오` 시트 손익이 평평하지 않다:

```
−100bp: −176,427원    0bp: +160,268원    +100bp: −131,652원
```

표준물(5% 쿠폰, 듀레이션 8.05년)이 바스켓(3%대 저쿠폰, 약 7.6년)보다 컨벡시티가 크다.
**선물매도 + 바스켓매수는 구조적으로 컨벡시티 매도 포지션**이며 BPV 헤지로 제거되지 않는다.
예시 입력 기준 **±70bp 부근에서 락인 차익이 전부 잠식**된다.

### 현재 입력값은 더미다

`실시간` 시트의 가격·수익률, `바스켓` 4~8행의 종목정보는 **형식 확인용 더미**이며
실거래 값이 아니다. 실전 투입 전 반드시 교체할 것.

### 이 모형이 반영하지 않는 것

- 휴일 캘린더 (`WORKDAY()` 기반이라 공휴일 미반영. `설정`에 수동 override 셀 있음)
- 선물 일일정산(변동증거금)의 이자효과 = 테일 조정
- 최종결제기준가격(딜러 호가 집계값)과 만기일 실제 체결 수익률의 괴리 — **가장 큰 잔여 리스크**
- 레포 롤오버 리스크, 담보 haircut, 기관 유형별 이자소득 원천징수

---

## 7. 다음 할 일

실전 투입 전 확정이 필요한 항목:

1. **해당 결제월 바스켓 3종목** — KRX 공시 최종결제기준채권 (종목코드/발행일/만기일/표면금리)
   → `바스켓` 시트 4~8행
2. **데이터 벤더 확정** — 인포맥스 / 블룸버그 / 증권사 API 중 무엇을 쓰는지에 따라
   `실시간` 시트 C열 수식이 달라진다. 벤더별 예시는 `연동가이드` 시트 A절 참조
3. **레포 term 금리** — 선물 만기까지의 조달금리. 오버나이트 롤이면 캐리 전제가 흔들린다
4. **거래비용 실측치** — 선물 위탁수수료(계약당), 채권 장외 스프레드, 최종결제 슬리피지
5. **KRX 상품명세 재확인** — 거래단위, 호가단위, 최종거래일 규칙 (`설정` B8/B10/B16)

3년물/5년물/30년물로 확장하려면 `설정` B4 드롭다운만 바꾸면 되지만,
바스켓 종목과 계약사양(특히 30년물)은 별도 확인이 필요하다.

바스켓 종목 수를 3개에서 늘리려면 `build_ktb_arb.py`의 `BLOCK_STARTS`와
`바스켓` 시트의 `CDE` 열 루프, `CFV` 매핑을 함께 수정해야 한다.
