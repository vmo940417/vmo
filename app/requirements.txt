fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
anthropic>=0.40
python-dotenv>=1.0
# 회사망 TLS 검사 장비 대응: Python 이 OS 인증서 저장소를 쓰게 한다.
# 없어도 앱은 뜨지만, 사내망에서는 인증서 오류로 시세 수집이 전부 실패한다.
truststore>=0.9
pytest>=8.0
pytest-asyncio>=0.24
