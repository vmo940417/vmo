fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
anthropic>=0.40
python-dotenv>=1.0
# 회사망 TLS 검사 장비 대응: Python 이 OS 인증서 저장소를 쓰게 한다.
# 없어도 앱은 뜨지만, 사내망에서는 인증서 오류로 시세 수집이 전부 실패한다.
truststore>=0.9
# KRX Data Marketplace 로그인 세션(개인 계정 KRX_ID/KRX_PW)으로 개별종목
# 공매도(MDCSTAT30001)를 받을 때만 쓴다 — 계정이 없으면 호출 자체가 안 되므로
# (server/providers/krx.py 에서 지연 import) 없어도 나머지 기능은 정상 동작한다.
pykrx>=1.0
pytest>=8.0
pytest-asyncio>=0.24
